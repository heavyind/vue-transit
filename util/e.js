


const cfgUndefined = "VueTransit requires configuration. Please pass a configuration object as the second argument to `Vue.use` (e.g., `Vue.use(VueTransit, cfg)`).";

const cfgStoreUndefined = "Your configuration object must include the Vuex store you intend to use (format `{ store: myStore }`).";

const cfgRouterUndefined = "Your configuration object must include the instance of Vue Router that you intend to use (format: `{ router: myRouter }`).";


export default {
  cfgUndefined,
  cfgStoreUndefined,
  cfgRouterUndefined
};
